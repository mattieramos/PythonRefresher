numbers = [5, 2, 5, 2, 2]

for number in numbers:
    print('x' * number) 

print('')

for number in numbers: #Not all languages have availability to print string multiplied by integers, so use nested for loop.
    output = ''
    for count in range(number):
        output += 'x'
    print(output)
